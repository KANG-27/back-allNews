const express = require("express");
const router = express.Router();
const axios = require("axios");

router.get("/", async (req, res) => {
  const { categoria, buscar } = req.query;
  try {
    // aqui va las apis de noticias
  } catch (error) {}
});
